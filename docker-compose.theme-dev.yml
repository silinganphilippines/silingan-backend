services:
  keycloak:
    environment:
      # Disable theme caches so CSS/template edits appear immediately in development.
      KC_SPI_THEME_CACHE_THEMES: "false"
      KC_SPI_THEME_CACHE_TEMPLATES: "false"
      KC_SPI_THEME_STATIC_MAX_AGE: "-1"